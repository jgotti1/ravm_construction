import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#ffab",
    },
    secondary: {
      main: "rgb(75, 112, 119)",
    },
  },
});
export default theme;
